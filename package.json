{
  "name": "garantinis",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run -w apps/api dev & npm run -w apps/web dev",
    "build": "npm run -w packages/db build && npm run -w apps/api build && npm run -w apps/web build",
    "lint": "npm run -w apps/api lint && npm run -w apps/web lint",
    "prisma:migrate": "npm run -w packages/db prisma:migrate",
    "prisma:seed": "npm run -w packages/db prisma:seed"
  }
}
